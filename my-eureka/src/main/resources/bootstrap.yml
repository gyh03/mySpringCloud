spring:
  cloud:
    config:
      uri: http://localhost:8888/
      enabled: true
      name: my-eureka,springCloudAndEureka #从config服务中获取相应的配置文件
      profile: default,default  #从config服务中相应profile位置获取配置信息
