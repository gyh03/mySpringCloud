#端口号
server:
  port: 8888
management:
  security:
    enabled: false #false为不认证，解决关于springboot-actuator监控的401无权限访问

spring:
  application:
    name: myConfigApp
  profiles:
    active: native
  config:
    name: application-native
# 注意 必须使用该配置启动项目，此配置不能少